<header class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
        <div class="container">
            <%= link_to "MANPOINTS", root_path, id: "logo" %>
            <nav>

                <div class="span3 pull-left">
                    <%= form_tag( search_user_path(current_user), method: 'get', class: "navbar-form" ) do %>
                        <div class="input-group">
                            <input type="text" class="form-control" placeholder="Search" name="srch-term" id="srch-term">
                            <!--<div class="input-group-btn">-->
                                <!--<button class="btn btn-default" type="submit"><i class="icon-search"></i></button>-->
                            <!--</div>-->
                        </div>
                    <% end %>
                </div>

                <ul class="nav pull-right">
                    <li><%= link_to "Home", root_path %></li>
                    <% if signed_in? %>
                        <li><%= link_to "Leagues", leagues_path %></li>
                        <li id="fat-menu" class="dropdown">
                            <a href="#" class="dropdown-toggle nav-clipped" data-toggle="dropdown">
                                <%= current_user.name %>
                                <!--<b class="caret"></b>-->
                            </a>
                            <ul class="dropdown-menu">
                                <li><%= link_to "Profile Settings", edit_user_path(current_user) %></li>
                                <li class="divider"></li>
                                <li>
                                    <%= link_to "Sign out", signout_path, method: "delete" %>
                                </li>
                            </ul>
                        </li>
                    <% else %>
                        <li><%= link_to "Sign in", signin_path %></li>
                    <% end %>
                </ul>
            </nav>
        </div>
    </div>
</header>