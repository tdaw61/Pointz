$("#friend_link_<%=@friend.id%>").replaceWith("<%= escape_javascript( link_to 'Cancel Friend Request',  friendship_path(id: @friend.id), method: :delete, remote: true, data: 'Are you sure you want to unfriend this person?', class: 'padding-left btn btn-sm btn-danger', id: "friend_link_#{@friend.id}" )%>");
