<% if signed_in? %>
    <div class="row">
        <div class="span4">

            <div class="user-info">
                <%= render 'shared/user_info' %>
            </div>
            <div class="padding-top">
                <%= render 'league_table' %>
            </div>
        </div>
        <div class="span8">
            <%= render 'shared/feed' %>
        </div>
    </div>
<% else %>


<%= render 'shared/splash_form' %>


<% end %>