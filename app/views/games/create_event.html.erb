<h1> Add/Subtract points to another User</h1>

<%= form_tag save_event_path do %>

    <p>
        <%= label_tag :user_id %><br>
        <%= text_field_tag :user_id %>

        <%= label_tag "points to +/-" %><br>
        <%= text_field_tag :point_value %>

        <%= label_tag "reason for the points" %><br>
        <%= text_field_tag :data %>
    </p>

    <%= submit_tag "Submit", class: "btn btn-large btn-primary" %>
<% end %>