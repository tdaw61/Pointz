<ul class="comments-list">
    <%= render 'comments/comment_form', {userpost: userpost} %>
    <% if !comments.empty? %>
        <%= render partial:'comments/comment', collection: comments || "empty collection" %>
    <% end %>
</ul>